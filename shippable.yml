language: python
python:
   - 2.7

build:
   pre_ci:
      - docker pull therickys93/ubuntu14
      - docker build --rm -t therickys93/ubuntu14ruby .

   pre_ci_boot:
      image_name: therickys93/ubuntu14ruby
      image_tag: latest
      pull: false
  
   ci:
     - bash /ruby/test/test.sh